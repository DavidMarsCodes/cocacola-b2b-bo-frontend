import { KaCurrencyPipe } from './ka-currency.pipe';

describe('KaCurrencyPipe', () => {
  it('create an instance', () => {
    // const pipe = new KaCurrencyPipe();
    // expect(pipe).toBeTruthy();
  });
});
